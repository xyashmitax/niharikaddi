#include <stdio.h>
#include <math.h>


int
main (void)
{
	int a, b, aa, bb, y;
printf("Enter value of side length 'a': ");
	scanf("%lf", &a);
	/* obtained value for a */
	
printf("Enter value of side length 'b': ");
	scanf("%lf", &b);
	/* obtained value for b */

aa = a * a;
	/* value for a*a */
	scanf("%d", &aa);

bb = b * b;
	/* value for b*b */
	scanf("%d", &bb);
	
y = aa + bb;
	/* value for c^2 */
	scanf("%d", &y);
	
printf("the value for y: \n");

}
